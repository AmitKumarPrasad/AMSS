spring.application.name=edusphere
spring.jpa.open-in-view=false
spring.servlet.multipart.max-file-size=${MAX_FILE_SIZE:25MB}
spring.servlet.multipart.max-request-size=${MAX_REQUEST_SIZE:30MB}
app.security.jwt-secret=${JWT_SECRET:change-me-in-production-change-me-in-production}
app.storage.local-root=${DOCUMENT_STORAGE_ROOT:./data/documents}
notifications.worker-id=${HOSTNAME:amss-worker}
notifications.delivery.fixed-delay-ms=${NOTIFICATIONS_DELIVERY_FIXED_DELAY_MS:30000}
